import 'package:flutter/material.dart';

/// Extra scroll padding inside donor tabs (bar is outside body when extendBody is false).
double donorScrollBottomInset(BuildContext context) {
  return 12;
}
